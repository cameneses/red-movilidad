const puppeteer = require('puppeteer');

// Función de scraping
async function getBusInfo(code) {
  // Lógica de scraping utilizando Puppeteer
  const results = { code };

  return results;
}

module.exports = { getBusInfo };
